<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.CertificateMapper">

    <insert id="insertCertificate" parameterType="com.example.model.Certificate">
        INSERT INTO certificates (user_no, doctor_no, certificate_type, issue_date, details, status)
        VALUES (
                       (SELECT user_no FROM users WHERE user_name = #{patientName}),
                       (SELECT doctor_no FROM doctors WHERE doctor_name = #{doctorName}),
                       #{certificateType}, #{issueDate}, #{details}, '신청'
               )
    </insert>
</mapper>
